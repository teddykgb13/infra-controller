# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

global:
  image:
    repository: ""
    tag: ""
    pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: image-pull-secret

replicaCount: 1

nameOverride: ""
namespaceOverride: ""

image:
  name: nico-mcp

service:
  type: ClusterIP
  port: 8080

# MCP server configuration. Set baseURL when forwarding inbound Authorization
# headers; a per-call base_url must match it. When baseURL is empty, only a
# token supplied in the same call may be sent to a per-call base_url. Tokens
# are intentionally NOT exposed here.
config:
  path: /mcp
  shutdownTimeout: 10s
  # -- Trusted NICo REST base URL (a per-call base_url must match when set)
  baseURL: ""
  # -- Optional server-side default org (a per-call org overrides it)
  org: ""
  # -- Optional API path segment used in /v2/org/<org>/<name>/... routes
  apiName: ""
  # -- Enable debug logging (full HTTP request/response)
  debug: false

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "250m"
