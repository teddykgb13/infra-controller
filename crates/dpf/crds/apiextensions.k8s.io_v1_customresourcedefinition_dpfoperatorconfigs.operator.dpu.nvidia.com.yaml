apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  labels:
    app.kubernetes.io/part-of: dpf-operator-controller-manager
    dpu.nvidia.com/component: dpf-operator-controller-manager
  name: dpfoperatorconfigs.operator.dpu.nvidia.com
spec:
  group: operator.dpu.nvidia.com
  names:
    kind: DPFOperatorConfig
    listKind: DPFOperatorConfigList
    plural: dpfoperatorconfigs
    singular: dpfoperatorconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DPFOperatorConfig is the Schema for the dpfoperatorconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DPFOperatorConfigSpec defines the desired state of DPFOperatorConfig
            properties:
              cniInstaller:
                description: CNIInstaller is the configuration for the cni-installer
                properties:
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  installer:
                    description: |-
                      Installer contains the configuration for the CNI-Installer component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                type: object
              deploymentMode:
                description: |-
                  DeploymentMode selects zero-trust vs host-trusted deployment alignment.
                  Required: operators must set this explicitly; provisioning controllers propagate this to DPU.status.deploymentMode.
                enum:
                - zero-trust
                - host-trusted
                type: string
              dpuDetector:
                description: DPUDetector is the configuration for the DPUDetector.
                properties:
                  daemon:
                    description: |-
                      Daemon contains the configuration for the DPU Detector component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  image:
                    description: |-
                      Image overrides the container image used by the DPUDetector Container.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `daemon` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'daemon.image' can
                    be set, but not both
                  rule: '!has(self.image) || !has(self.daemon) || !has(self.daemon.image)'
              dpuServiceController:
                description: DPUServiceController is the configuration for the DPUServiceController
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the DPU Service controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  disableDPUReadyTaints:
                    description: |-
                      DisableDPUReadyTaints disables the DPU ready taints feature in the DPU Service Controller.
                      This feature adds taints to the worker nodes when the DPU is not ready.
                      This is useful when the DPU is used for networking and the node should not be scheduled until the DPU is ready.
                    type: boolean
                  image:
                    description: |-
                      Image overrides the container image used by the DPUService controller.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
              flannel:
                description: Flannel is the configuration for Flannel
                properties:
                  cni:
                    description: |-
                      CNI is the configuration for the Flannel CNI component.
                      It contains the image for the CNI init container.
                      Note: The resources for the CNI container are not configurable.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                    type: object
                  daemon:
                    description: |-
                      Daemon is the configuration for the Flannel Daemon component.
                      It contains the image for the Flannel Daemon container and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Images overrides the container images used by flannel

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new fields `cni` and `daemon` instead.
                    properties:
                      flannelCNI:
                        description: FlannelCNI must be set if FlannelImages is set.
                        minLength: 1
                        type: string
                      kubeFlannel:
                        description: KubeFlannel must be set if FlannelImages is set.
                        minLength: 1
                        type: string
                    required:
                    - flannelCNI
                    - kubeFlannel
                    type: object
                  podCIDR:
                    description: PodCIDR is the pod cidr for flannel.
                    type: string
                type: object
              imagePullSecrets:
                description: |-
                  List of secret names which are used to pull images for DPF system components and DPUServices.
                  These secrets must be in the same namespace as the DPF Operator Config and should be created before the config is created.
                  System reconciliation will not proceed until these secrets are available.
                items:
                  type: string
                type: array
              kamajiClusterManager:
                description: KamajiClusterManager is the configuration for the kamaji-cluster-manager
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the Kamaji Cluster Manager component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  image:
                    description: |-
                      Image overrides the container image used by the Kamaji Cluster Manager.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
              monitoring:
                description: Monitoring is the configuration for monitoring resources.
                properties:
                  disable:
                    description: |-
                      Disable controls whether monitoring resources are installed.
                      When enabled (default), the controller:
                      - Creates ServiceMonitors for Kamaji clusters to scrape control-plane metrics.
                      - Deploys kube-state-metrics as a DPUService to expose metrics for custom resources.
                      - Deploys node-problem-detector as a DaemonSet on DPU nodes to detect and report node-level problems.
                      - Deploys opentelemetry-collector as a DaemonSet on DPU nodes to collect and forward logs.
                    type: boolean
                  kubeStateMetrics:
                    description: KubeStateMetrics is the configuration for kube-state-metrics
                    properties:
                      daemon:
                        description: |-
                          Daemon contains the configuration for the kube-state-metrics component.
                          It contains the image for kube-state-metrics and its resource requirements.
                        properties:
                          image:
                            description: Image is a reference to a container image.
                            pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                            type: string
                          resources:
                            description: |-
                              Resources defines the memory and CPU resource requests and limits for the component.
                              This field is optional, and if not set, the component will use the default resource.
                            properties:
                              limits:
                                description: Limits defines the resource limits for
                                  the component.
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: CPU is the amount of CPU requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Memory is the amount of Memory requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                              requests:
                                description: Requests defines the resource requests
                                  for the component.
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: CPU is the amount of CPU requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Memory is the amount of Memory requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                            type: object
                        type: object
                      disable:
                        description: Disable ensures the component is not deployed
                          when set to true.
                        type: boolean
                      helmChart:
                        description: |-
                          HelmChart overrides the helm chart used by the ServiceSet controller.
                          The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                          OCI registry or a web-based repository.
                        pattern: ^(oci://|https://).+$
                        type: string
                    type: object
                  nodeProblemDetector:
                    description: NodeProblemDetector is the configuration for node-problem-detector
                    properties:
                      daemon:
                        description: |-
                          Daemon contains the configuration for the node-problem-detector component.
                          It contains the image for node-problem-detector and its resource requirements.
                        properties:
                          image:
                            description: Image is a reference to a container image.
                            pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                            type: string
                          resources:
                            description: |-
                              Resources defines the memory and CPU resource requests and limits for the component.
                              This field is optional, and if not set, the component will use the default resource.
                            properties:
                              limits:
                                description: Limits defines the resource limits for
                                  the component.
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: CPU is the amount of CPU requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Memory is the amount of Memory requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                              requests:
                                description: Requests defines the resource requests
                                  for the component.
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: CPU is the amount of CPU requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Memory is the amount of Memory requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                            type: object
                        type: object
                      disable:
                        description: Disable ensures the component is not deployed
                          when set to true.
                        type: boolean
                      helmChart:
                        description: |-
                          HelmChart overrides the helm chart used by the ServiceSet controller.
                          The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                          OCI registry or a web-based repository.
                        pattern: ^(oci://|https://).+$
                        type: string
                    type: object
                  openTelemetryCollector:
                    description: OpenTelemetryCollector is the configuration for opentelemetry-collector
                    properties:
                      daemon:
                        description: |-
                          Daemon contains the configuration for the opentelemetry-collector component.
                          It contains the image for opentelemetry-collector and its resource requirements.
                        properties:
                          image:
                            description: Image is a reference to a container image.
                            pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                            type: string
                          resources:
                            description: |-
                              Resources defines the memory and CPU resource requests and limits for the component.
                              This field is optional, and if not set, the component will use the default resource.
                            properties:
                              limits:
                                description: Limits defines the resource limits for
                                  the component.
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: CPU is the amount of CPU requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Memory is the amount of Memory requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                              requests:
                                description: Requests defines the resource requests
                                  for the component.
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: CPU is the amount of CPU requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Memory is the amount of Memory requested
                                      by the component.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                            type: object
                        type: object
                      disable:
                        description: Disable ensures the component is not deployed
                          when set to true.
                        type: boolean
                      helmChart:
                        description: |-
                          HelmChart overrides the helm chart used by the ServiceSet controller.
                          The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                          OCI registry or a web-based repository.
                        pattern: ^(oci://|https://).+$
                        type: string
                      logging:
                        description: |-
                          Logging contains the configuration for the opentelemetry-collector logging component.
                          If not specified, logging will not be streamed.
                        properties:
                          endpoint:
                            description: |-
                              Endpoint is the OTLP endpoint where the DPU cluster opentelemetry-collector sends data to.
                              This could be the management cluster's opentelemetry-collector endpoint.
                              If not specified, nothing will be forwarded from DPU clusters.
                            type: string
                        required:
                        - endpoint
                        type: object
                    type: object
                type: object
              multus:
                description: Multus is the configuration for Multus
                properties:
                  cni:
                    description: |-
                      CNI contains the configuration for the Multus CNI component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the Multus Container.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `cni` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'cni.image' can be
                    set, but not both
                  rule: '!has(self.image) || !has(self.cni) || !has(self.cni.image)'
              networking:
                default:
                  controlPlaneMTU: 1500
                description: Networking defines the networking configuration for the
                  system components.
                properties:
                  controlPlaneMTU:
                    default: 1500
                    description: |-
                      ControlPlaneMTU is the MTU value to be set on the management network.
                      In zero-trust mode this value is applied to the DPU OOB interface (oob_net0), which does not
                      support jumbo frames; it must not exceed 1500 when deploymentMode is zero-trust.
                      The default is 1500.
                    maximum: 9216
                    minimum: 1280
                    type: integer
                  dpuNodeOOBBridgeName:
                    default: br-dpu
                    description: |-
                      DPUNodeOOBBridgeName is the name of the Linux bridge on the host used for
                      out-of-band DPU management traffic. If not specified, defaults to "br-dpu".
                      This setting applies only to host-trusted deployments.
                    maxLength: 15
                    minLength: 1
                    pattern: ^[a-z][a-z0-9-]*$
                    type: string
                  highSpeedMTU:
                    default: 1500
                    description: |-
                      HighSpeedMTU is the MTU value to be set on the high-speed interface.
                      The default is 1500.
                    maximum: 9216
                    minimum: 1280
                    type: integer
                type: object
              nodeSRIOVDevicePluginController:
                description: |-
                  NodeSRIOVDevicePluginController is the configuration for the NodeSRIOVDevicePlugin controller.
                  This controller manages per-node SRIOV device plugin pods based on DPU configurations.
                  The controller is disabled by default.
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the NodeSRIOVDevicePlugin controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  devicePlugin:
                    description: |-
                      DevicePlugin contains the configuration for the SRIOV device plugin pods
                      managed by this controller.
                    properties:
                      defaultResourcePrefix:
                        description: |-
                          DefaultResourcePrefix is the default resource prefix for the SRIOV device plugin resources.
                          Defaults to "nvidia.com".
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      image:
                        description: Image overrides the container image for the SRIOV
                          device plugin.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      initImage:
                        description: |-
                          InitImage overrides the container image for the init container
                          that generates device plugin configuration.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
              nvipam:
                description: NVIPAM is the configuration for NVIPAM
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the NVIPAM controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the NVIPAM controller.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  node:
                    description: |-
                      Node contains the configuration for the NVIPAM node component.
                      It contains the image for the node and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
              overrides:
                description: |-
                  Overrides exposes a set of fields which impact the recommended behavior of the DPF Operator.
                  These fields should only be set for advanced use cases. The fields here have no stability guarantees.
                properties:
                  argoCDNamespace:
                    description: |-
                      ArgoCDNamespace is the namespace where ArgoCD is deployed.
                      AppProjects and cluster secrets required by DPF will be created in this namespace.
                      Defaults to the namespace of the DPFOperatorConfig.
                    maxLength: 63
                    minLength: 1
                    type: string
                  dpuCNIBinPath:
                    description: |-
                      DPUCNIBinPath is the path at which the CNI binaries will be installed to on the DPU.
                      This is /opt/cni/bin by default.
                      This setting does not change where kubelet is configured to use the CNI from.
                    type: string
                  dpuCNIPath:
                    description: |-
                      DPUCNIConfigPath is the path to which the CNI config files will be installed on the DPU.
                      This is /etc/cni/net.d by default.
                      This setting does not change where kubelet is configured to read the CNI config from.
                    type: string
                  dpuLinkerCachePath:
                    description: |-
                      DPULinkerCachePath is the path on the DPU at which the prebuilt dynamic-linker cache
                      file can be found. When set, this file is mounted read-only into the SFC Controller
                      container so that host OVS binaries can resolve shared libraries using the DPU's
                      linker configuration. If not set, no linker cache mount is added.
                      This setting does not change where components are installed. Installation location fixed in the BFB.
                    minLength: 1
                    type: string
                  dpuOpenvSwitchBinPath:
                    description: |-
                      DPUOpenvSwitchBinPath is the path at which the openvSwitch bin directory can be found on the DPU node.
                      This is /usr/bin/ by default.
                      This setting does not change where components are installed. Installation location fixed in the BFB.
                    type: string
                  dpuOpenvSwitchRunPath:
                    description: |-
                      DPUOpenvSwitchPath is the path at which the openvSwitch run directory can be found on the DPU.
                      This is /var/run/openvswitch by default.
                      This setting does not change where components are installed. Installation location fixed in the BFB.
                    type: string
                  dpuOpenvSwitchSystemSharedLib64Path:
                    description: |-
                      DPUOpenvSwitchSystemSharedLib64Path is the path at which the system lib64 used by OVS components can be found on the DPU.
                      If this field is not set, no lib64 volume mount will be configured in the SFC Controller component.
                      This setting does not change where components are installed. Installation location fixed in the BFB.
                    minLength: 1
                    type: string
                  dpuOpenvSwitchSystemSharedPath:
                    description: |-
                      DPUOpenvSwitchSystemSharedLibPath is the path at which the system lib used by OVS components can be found on the DPU.
                      This is /lib by default.
                      This setting does not change where components are installed. Installation location fixed in the BFB.
                    type: string
                  dpuOptLibraryPath:
                    description: |-
                      DPUOptLibraryPath is the path on the DPU at which an additional library directory
                      can be found. When set, this directory is mounted read-only into the SFC Controller
                      container. Useful on distributions that install vendor libraries outside the standard
                      paths (e.g. /usr/opt on RHCOS BFB). If not set, no additional library directory is mounted.
                      This setting does not change where components are installed. Installation location fixed in the BFB.
                    minLength: 1
                    type: string
                  flannelSkipCNIConfigInstallation:
                    description: |-
                      FlannelSkipCNIConfigInstallation controls whether Flannel should skip CNI config installation.
                      This is true by default, meaning Flannel does not manage its own CNI configuration.
                      Set to false if you want Flannel to install a CNI configuration.
                    type: boolean
                  kubernetesAPIServerPort:
                    description: |-
                      KubernetesAPIServerPort is the port the Kubernetes API server is accessible at.
                      This setting is usually used together with the kubernetesAPIServerVIP setting. It enables specific underlying
                      components deployed directly or indirectly by the DPF Operator to reach the Kubernetes API Server when the
                      ClusterIP Kubernetes Service is not functional.
                    type: integer
                  kubernetesAPIServerVIP:
                    description: |-
                      KubernetesAPIServerVIP is the VIP the Kubernetes API server is accessible at.
                      This setting enables specific underlying components deployed directly or indirectly by the DPF Operator to reach
                      the Kubernetes API Server when the ClusterIP Kubernetes Service is not functional.
                      If set, it should be set to an IP to ensure that components work even if DNS is not available in the cluster.
                    type: string
                  paused:
                    description: Paused disables all reconciliation of the DPFOperatorConfig
                      when set to true.
                    type: boolean
                type: object
              ovsCNI:
                description: |-
                  OVSCNI is the configuration for OVSCNI

                  Deprecated: OVS CNI is installed by CNIInstaller. Remove after 26.7 is released.
                properties:
                  cni:
                    description: |-
                      CNI contains the configuration for the OVS CNI component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the OVS CNI.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `cni` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'cni.image' can be
                    set, but not both
                  rule: '!has(self.image) || !has(self.cni) || !has(self.cni.image)'
              provisioningController:
                description: ProvisioningController is the configuration for the ProvisioningController
                properties:
                  bfCFGTemplateConfigMap:
                    description: |-
                      BFCFGTemplateConfigMap is the name of a configMap containing a template for the BF.cfg file used by the DPU controller.
                      By default the provisioning controller use a hardcoded BF.cfg e.g. https://github.com/NVIDIA/doca-platform/blob/release-v24.10/internal/provisioning/controllers/dpu/bfcfg/bf.cfg.template
                      Note: Replacing the bf.cfg is an advanced use case. The default bf.cfg is designed for most use cases.

                      Deprecated: BFCFGTemplateConfigMap is deprecated and will be removed in a future release.
                      Use enableDynamicBFCFGTemplates instead for custom bf.cfg templates.
                    type: string
                  bfbPVCName:
                    description: |-
                      BFBPersistentVolumeClaimName is the name of the PersistentVolumeClaim used by dpf-provisioning-controller
                      If not provided, the controller will use local host storage (hostPath)
                    type: string
                  controller:
                    description: |-
                      Controller contains the configuration for the Provisioning controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  customCASecretName:
                    description: |-
                      CustomCASecretName indicates the name of the Kubernetes secret object
                      which containing the custom CA certificate
                    type: string
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  dmsTimeout:
                    description: DMSTimeout is the max time in seconds within which
                      a DMS API must respond, 0 is unlimited
                    minimum: 1
                    type: integer
                  enableDynamicBFCFGTemplates:
                    description: |-
                      EnableDynamicBFCFGTemplates enables runtime discovery of bf.cfg templates via ConfigMaps.
                      When enabled, the provisioning controller discovers ConfigMaps by matching labels for BFB
                      name/namespace and DPUCluster name/namespace. Mutually exclusive with bfCFGTemplateConfigMap.
                    type: boolean
                  firmwareUpdateTimeout:
                    description: |-
                      FirmwareUpdateTimeout is the maximum time allowed for BF4 firmware update in zero-trust mode.
                      If the update exceeds this timeout, the DPU will transition to an error state.
                      When unset, the provisioning controller defaults to 45m.
                    format: duration
                    pattern: ^([0-9]+(h|m|s|ms|us|µs|ns))+$
                    type: string
                  hostAgentDNSPolicy:
                    description: |-
                      HostAgentDNSPolicy sets the DNS policy for the hostagent pod.
                      Valid values are 'ClusterFirstWithHostNet', 'ClusterFirst', 'Default' or 'None'.
                      Defaults to 'ClusterFirstWithHostNet'.
                    enum:
                    - ClusterFirstWithHostNet
                    - ClusterFirst
                    - Default
                    - None
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the Provisioning controller.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  installInterface:
                    description: InstallInterface is the interface through which the
                      BFB is installed
                    properties:
                      installViaGNOI:
                        description: |-
                          InstallViaGNOI is the interface used to install the BFB via GNOI

                          Deprecated: Use InstallViaHostAgent instead.
                        type: object
                      installViaHostAgent:
                        description: InstallViaHostAgent is the interface used to
                          install the BFB via HostAgent
                        type: object
                      installViaRedfish:
                        description: InstallViaRedfish is the interface used to install
                          the BFB via Redfish
                        properties:
                          bfbRegistry:
                            description: |-
                              BFBRegistry is the configuration for the BFB Registry

                              Deprecated: Use RegistryConfiguration instead.
                            properties:
                              disable:
                                description: Disable ensures the BFB Registry is not
                                  deployed when set to true.
                                type: boolean
                              port:
                                description: Port is the port on which the BFB Registry
                                  will listen
                                type: integer
                            type: object
                          bfbRegistryAddress:
                            description: |-
                              BFBRegistryAddress is the address of the BFB Registry

                              Deprecated: Use RegistryConfiguration instead.
                            minLength: 1
                            type: string
                          skipDPUNodeDiscovery:
                            default: true
                            description: SkipDPUNodeDiscovery is a flag to skip the
                              DPU node discovery.
                            type: boolean
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of installViaHostAgent, installViaGNOI
                        or installViaRedfish must be set
                      rule: ((has(self.installViaHostAgent) || has(self.installViaGNOI))
                        && !has(self.installViaRedfish)) || (!has(self.installViaHostAgent)
                        && !has(self.installViaGNOI) && has(self.installViaRedfish))
                  maxDPUParallelInstallations:
                    default: 50
                    description: |-
                      MaxDPUParallelInstallations specifies the maximum number of DPUs that can be provisioned concurrently.
                      A DPU is removed from the concurrent provisioning count as soon as it finishes the "OS Installing" phase and
                      enters the "Rebooting" phase of its provisioning lifecycle.
                    format: int32
                    minimum: 1
                    type: integer
                  maxUnavailableDPUNodes:
                    default: 50
                    description: MaxUnavailableDPUNodes is the maximum number of DPUNodes
                      that are unavailable during the node effect period.
                    format: int32
                    minimum: 1
                    type: integer
                  multiDPUOperationsSyncWaitTime:
                    default: 30s
                    description: |-
                      MultiDPUOperationsSyncWaitTime is the wait time between DPUs sync operations on the same node.
                      It would take effect only on DPUNode objects which contain more than one DPU.
                    format: duration
                    pattern: ^([0-9]+(h|m|s|ms|us|µs|ns))+$
                    type: string
                  nodeEffectRemovalTimeout:
                    description: |-
                      NodeEffectRemovalTimeout is the maximum time allowed for the Node Effect Removal phase.
                      If the DPUNodeMaintenance CR still has requestors after this timeout, the DPU will transition to an error state.
                      When unset, the provisioning controller defaults to 0s (timeout disabled).
                    format: duration
                    pattern: ^([0-9]+(h|m|s|ms|us|µs|ns))+$
                    type: string
                  osInstallTimeout:
                    description: |-
                      OSInstallTimeout is the maximum time allowed for OS installation in zero-trust mode.
                      If the installation exceeds this timeout, the DPU will transition to an error state.
                      When unset, the provisioning controller defaults to 60m.
                    format: duration
                    pattern: ^([0-9]+(h|m|s|ms|us|µs|ns))+$
                    type: string
                  registry:
                    description: Registry is the configuration for the BFB Registry
                    properties:
                      address:
                        description: |-
                          Address is the address used to access the BFB Registry. The address must start with "http://".
                          By default, the BFB Registry can be accessed via its Service.
                          For non-kubernetes environments, this must be set due to the lack of kubelet on worker nodes.
                          For zero-trust environments, this must be set so that the BFB Registry can be accessed from DPU BMC.
                          Deprecated: Address is deprecated and will be removed in a future release.
                        pattern: ^http://
                        type: string
                      loadBalancerAddress:
                        description: |-
                          LoadBalancerAddress is the address of the load balancer for the BFB Registry which the hostagent/redfish use to fetch the BFB and generated bf.cfg.
                          To enable the load balancer, you need to deploy your own load balancer controller and configure the LoadBalancerAddress field.
                          Then check the bfb-registry nodeport service and make your load balancer controller to distribute the requests to the bfb-registry nodeport.
                        pattern: ^http://
                        type: string
                      port:
                        description: |-
                          Port is the port on which the registry instances will listen
                          Deprecated: Address is deprecated and will be removed in a future release.
                        maximum: 65535
                        minimum: 1
                        type: integer
                    type: object
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
                - message: bfCFGTemplateConfigMap and enableDynamicBFCFGTemplates
                    are mutually exclusive
                  rule: '!(has(self.bfCFGTemplateConfigMap) && has(self.enableDynamicBFCFGTemplates)
                    && self.enableDynamicBFCFGTemplates)'
              security:
                description: Security groups configuration for security-related components
                  managed by the DPF Operator.
                properties:
                  kata:
                    description: |-
                      Kata is the configuration for Kata Containers.
                      Kata Containers provides VM-based isolation for untrusted workloads on DPU nodes.
                      This component is disabled by default; set disable to false to enable.
                    properties:
                      containerdConfigFileName:
                        description: |-
                          ContainerdConfigFileName overrides the containerd config file name
                          on the target nodes. Defaults to "config-mlnx.toml".
                        type: string
                      daemon:
                        description: |-
                          Daemon contains the configuration for the kata-deploy component.
                          It contains the image for the kata-deploy container.
                        properties:
                          image:
                            description: Image is a reference to a container image.
                            pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                            type: string
                        type: object
                      disable:
                        description: Disable ensures the component is not deployed
                          when set to true.
                        type: boolean
                      helmChart:
                        description: |-
                          HelmChart overrides the helm chart used by the ServiceSet controller.
                          The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                          OCI registry or a web-based repository.
                        pattern: ^(oci://|https://).+$
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: |-
                          NodeSelector restricts which nodes kata-deploy runs on.
                          This is passed as the Helm chart's nodeSelector value.
                        type: object
                      shims:
                        description: |-
                          Shims selects which Kata hypervisor shims to enable.
                          Defaults to ["qemu"] if empty.
                        items:
                          allOf:
                          - enum:
                            - qemu
                          - enum:
                            - qemu
                          description: |-
                            KataShim identifies a Kata hypervisor shim variant.
                            Values must match the shim keys in the kata-deploy Helm chart's
                            `shims.<name>.enabled` values.
                            Only arm64-compatible shims are supported.
                          type: string
                        type: array
                    type: object
                  privilegedPodEnforcement:
                    default: true
                    description: |-
                      PrivilegedPodEnforcement controls whether privileged pods are rejected
                      unless explicitly allowed by the workload API. The DPUService controller
                      currently implements this by applying the PrivilegedPodEnforcement
                      ValidatingAdmissionPolicy to DPUService workloads.

                      Setting it to false does not fully opt out of enforcement: the policy and its
                      binding are kept, but the binding is switched from Deny to Audit, so privileged
                      pods are no longer denied and are only recorded in the audit log. The allowlist
                      is kept populated so the audit log only flags pods that would otherwise be
                      denied.

                      The objects are intentionally not deleted to avoid a Kubernetes paramRef
                      informer bug (https://github.com/kubernetes/kubernetes/issues/133827).

                      Defaults to true.
                    type: boolean
                  spiffe:
                    description: |-
                      spiffe configures the SPIFFE-based DPU Agent identity flow. Edits are accepted post-bootstrap
                      but do NOT retro-apply to already-provisioned DPUs.
                    properties:
                      kubeAPIAudience:
                        description: |-
                          KubeAPIAudience is the audience claim the DPU Agent's JWT-SVID must carry; it must match an
                          entry in the kube-apiserver AuthenticationConfiguration.audiences[] (owned out-of-band).
                        maxLength: 512
                        minLength: 1
                        type: string
                      spireOIDCURL:
                        description: |-
                          SPIREOIDCURL is the OIDC discovery (issuer) URL of the pre-installed SPIRE Server.
                          The matching kube-apiserver AuthenticationConfiguration.jwt[].issuer value is applied out-of-band.
                        maxLength: 2048
                        minLength: 1
                        type: string
                      spireServerAddress:
                        description: |-
                          SPIREServerAddress is the address of the pre-installed SPIRE Server in host:port form
                          (e.g. "spire-server.spire-system.svc:8081").
                        maxLength: 263
                        minLength: 1
                        type: string
                      spireTrustDomain:
                        description: |-
                          SPIRETrustDomain is the SPIRE-internal trust domain (e.g. "cs.internal") embedded in the
                          DPU Agent SVID URI.
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      trustBundle:
                        description: |-
                          trustBundle references a ConfigMap whose data["bundle.pem"] key holds the SPIRE trust
                          bundle in PEM form.
                        properties:
                          name:
                            description: Name is the name of the ConfigMap holding
                              the SPIRE trust bundle.
                            maxLength: 253
                            minLength: 1
                            type: string
                          namespace:
                            description: Namespace is the namespace of the ConfigMap
                              holding the SPIRE trust bundle.
                            maxLength: 63
                            minLength: 1
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    required:
                    - kubeAPIAudience
                    - spireOIDCURL
                    - spireServerAddress
                    - spireTrustDomain
                    - trustBundle
                    type: object
                    x-kubernetes-validations:
                    - message: spireServerAddress must be host:port with a valid DNS-1123
                        host (e.g. spire-server.spire-system.svc:8081)
                      rule: self.spireServerAddress.matches('^[A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?([.][A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?)*:[1-9][0-9]{0,4}$')
                    - message: spireServerAddress port must be in 1-65535
                      rule: '!self.spireServerAddress.contains('':'') || (int(self.spireServerAddress.split('':'')[1])
                        >= 1 && int(self.spireServerAddress.split('':'')[1]) <= 65535)'
                type: object
              serviceSetController:
                description: ServiceSetController is the configuration for the ServiceSetController
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the ServiceChainSet controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the ServiceChainSet Controller.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
              sfcController:
                description: SFCController is the configuration for the SFCController
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the SFC controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the SFC controller.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  secureFlowDeletionTimeout:
                    description: |-
                      SecureFlowDeletionTimeout controls the timeout for which the API server is unreachable after which all the flows
                      are deleted to prevent unintended packet leaks. It has effect when is greater than zero.
                      Value must be in units accepted by Go time.ParseDuration https://golang.org/pkg/time/#ParseDuration.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
              sriovDevicePlugin:
                description: SRIOVDevicePlugin is the configuration for the SRIOVDevicePlugin
                properties:
                  deviceplugin:
                    description: |-
                      DevicePlugin contains the configuration for the SRIOV Device Plugin component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  helmChart:
                    description: |-
                      HelmChart overrides the helm chart used by the ServiceSet controller.
                      The URL must begin with either 'oci://' or 'https://', ensuring it points to a valid
                      OCI registry or a web-based repository.
                    pattern: ^(oci://|https://).+$
                    type: string
                  image:
                    description: |-
                      Image overrides the container image used by the SRIOV Device Plugin container.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `deviceplugin` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'deviceplugin.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.deviceplugin) || !has(self.deviceplugin.image)'
              staticClusterManager:
                description: StaticClusterManager is the configuration for the static-cluster-manager
                properties:
                  controller:
                    description: |-
                      Controller contains the configuration for the Static Cluster Manager controller component.
                      It contains the image for the controller and its resource requirements.
                    properties:
                      image:
                        description: Image is a reference to a container image.
                        pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                        type: string
                      resources:
                        description: |-
                          Resources defines the memory and CPU resource requests and limits for the component.
                          This field is optional, and if not set, the component will use the default resource.
                        properties:
                          limits:
                            description: Limits defines the resource limits for the
                              component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          requests:
                            description: Requests defines the resource requests for
                              the component.
                            properties:
                              cpu:
                                anyOf:
                                - type: integer
                                - type: string
                                description: CPU is the amount of CPU requested by
                                  the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              memory:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Memory is the amount of Memory requested
                                  by the component.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                        type: object
                    type: object
                  disable:
                    description: Disable ensures the component is not deployed when
                      set to true.
                    type: boolean
                  image:
                    description: |-
                      Image overrides the container image used by the Static Cluster Manager.

                      Deprecated: This field is deprecated and will be removed with v26.7.0.
                      Use the new field `controller` instead.
                    pattern: ^((?:(?:(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])(?:\.(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))*|\[(?:[a-fA-F0-9:]+)\])(?::[0-9]+)?/)?[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*(?:/[a-z0-9]+(?:(?:[._]|__|[-]+)[a-z0-9]+)*)*)(?::([\w][\w.-]{0,127}))?(?:@([A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*[:][[:xdigit:]]{32,}))?$
                    type: string
                  replicas:
                    default: 2
                    description: |-
                      Replicas is the number of replicas for the controller deployment.
                      Used for High Availability via leader election.
                    format: int32
                    maximum: 3
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: only either 'image' (deprecated) or 'controller.image'
                    can be set, but not both
                  rule: '!has(self.image) || !has(self.controller) || !has(self.controller.image)'
            required:
            - deploymentMode
            - provisioningController
            type: object
            x-kubernetes-validations:
            - message: deploymentMode zero-trust requires provisioningController.installInterface.installViaRedfish
              rule: self.deploymentMode != 'zero-trust' || (has(self.provisioningController.installInterface)
                && has(self.provisioningController.installInterface.installViaRedfish))
            - message: deploymentMode host-trusted does not support provisioningController.installInterface.installViaRedfish
              rule: self.deploymentMode != 'host-trusted' || !has(self.provisioningController.installInterface)
                || !has(self.provisioningController.installInterface.installViaRedfish)
            - message: dpuNodeOOBBridgeName is only configurable in host-trusted mode
              rule: self.deploymentMode == 'host-trusted' || !has(self.networking.dpuNodeOOBBridgeName)
                || self.networking.dpuNodeOOBBridgeName == 'br-dpu'
            - message: controlPlaneMTU must not exceed 1500 in zero-trust mode because
                DPU OOB interfaces do not support jumbo frames
              rule: self.deploymentMode != 'zero-trust' || !has(self.networking) ||
                !has(self.networking.controlPlaneMTU) || self.networking.controlPlaneMTU
                <= 1500
            - message: spiffe configuration requires deploymentMode=zero-trust
              rule: '!has(self.security) || !has(self.security.spiffe) || self.deploymentMode
                == ''zero-trust'''
            - message: spec.security.spiffe cannot be removed once set; SPIFFE-mode
                DPUs depend on this configuration. Disable SPIFFE for the cluster
                by deleting and recreating DPFOperatorConfig (DR escape hatch); existing
                SPIFFE-mode DPUs require re-provisioning.
              rule: '!has(oldSelf.security) || !has(oldSelf.security.spiffe) || (has(self.security)
                && has(self.security.spiffe))'
          status:
            description: DPFOperatorConfigStatus defines the observed state of DPFOperatorConfig
            properties:
              conditions:
                description: Conditions exposes the current state of the OperatorConfig.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration records the Generation observed on
                  the object the last time it was patched.
                format: int64
                type: integer
              version:
                description: Version is the version of the DPF Operator that is currently
                  deployed.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
